<ul class="grid grid-cols-1 md:grid-cols-3 items-center gap-y-10">
    <slot />
</ul>
<style>
    ul {
        opacity: 0;
        animation: fadeIn ease-in-out forwards;
        animation-timeline: view();
        animation-range: 250px 500px;
    }

    @keyframes fadeIn {
        to {
            opacity: 1;
        }
    }
</style>
